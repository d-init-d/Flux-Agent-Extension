// ---------------------------------------------------------------------------
// Flux Agent Extension - Theme System
// Barrel export
// ---------------------------------------------------------------------------

export { ThemeProvider, useTheme } from './ThemeProvider';
export type {
  ThemeMode,
  ResolvedTheme,
  ThemeContextValue,
  ThemeProviderProps,
} from './ThemeProvider';
